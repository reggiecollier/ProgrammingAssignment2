## This is a short comment describing the function

makeCacheMatrix <- function(x = matrix()) {
  x <- numeric()

}


## This short comment describes the other function

cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
}

